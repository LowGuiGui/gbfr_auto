# Runtime dependencies for gbfr_auto.
#
# Direct dependencies only, exactly pinned. Versions were resolved and import-
# verified on Linux (CPython 3.13) on 2026-08-23; Windows is the actual target
# platform, so re-verify there before relying on these pins.
#
# For a fully-pinned lock including transitives:
#     uv pip compile requirements.txt -o requirements.lock

numpy==2.5.2
# 2026-08-26 实测过 4.14.0.94 和 5.0.0.93（#9）：
#   - 依赖 opencv 的 39 个测试在两边都通过
#   - 五张真实模板贴进随机场景后，匹配分数和坐标**逐位相同**
#     （flag_battle 两边都是 0.999999404 @ (200,100)，其余同理）
# 也就是说，本仓库用到的那 13 个 cv2 符号跨大版本没有行为差异，pin 选哪个都不
# 影响结果。留在 5.0.0.93 是因为它是当前最新，而不是因为 4.x 过时 —— 4.x 仍在
# 并行维护（4.14.0.94 的构建号比 5.0.0.93 还高，发布更晚）。
#
# 下次升级要检查的就是那 13 个符号，测试 test_opencv.py 里钉住了它们。
opencv-python==5.0.0.93
pillow==12.3.0
pyautogui==0.9.54
pynput==1.8.2

# Windows-only. This branch uses win32 APIs for window enumeration and capture
# (window_capture.py), background input injection (window_input.py), and the
# hook DLL's named-pipe channel (hook/injector.py).
#
# pywin32 publishes no non-Windows wheels at all, so the environment marker is
# load-bearing: without it `pip install -r requirements.txt` fails outright on
# Linux, which would block editor tooling and local testing of opencv.py.
pywin32==312 ; sys_platform == "win32"
