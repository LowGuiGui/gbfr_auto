### Python ###
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

### PyInstaller ###
build/
dist/
*.spec

### 虚拟环境 ###
venv/
env/
.venv/
.env/

### IDE / 编辑器 ###
.vscode/
.idea/
*.swp
*.swo
*~

### 系统文件 ###
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
.DS_Store
Desktop.ini

### 日志 ###
*.log
logs/

### 临时文件 ###
*.tmp
*.bak
*.cache

### 运行时生成 ###
# 从源码运行时 exe_dir() 就是仓库根目录，配置文件会落在这里。内容是本机设置。
gbfr_auto.toml
anomalies/
# 从源码运行时 exe_dir() 就是仓库根目录，模板清单会落进被跟踪的 template/。
# 记录的是本机状态，不该进版本库；*.new 是升级时给用户对比用的新版内置模板。
template/.bundled.json
template/*.new

### 编译产物 ###
# 从 CI 下载解压的发行包放这里，别提交回去（就是 #6 要避免的那种事）。
gbfr-build/

# CI 构建时下载解包的 ViGEm 二进制（ViGEmClient.dll / ViGEmBusSetup_x64.msi）。
# 和 hook DLL 同一条规矩：二进制不进版本库，构建时取。
vigem/
vgamepad.tar.gz
vgamepad-*/

# hook/gbfr_hook.dll 由 hook/gbfr_hook.c 编译生成 (hook/build.bat 或 CI)。
# 二进制无法 diff、无法审阅，且上游正是因为提交构建产物，才留下 ~96 MB 的
# 历史记录挂在 ~450 KB 的工作树后面。cl /LD 的中间产物一并忽略。
hook/gbfr_hook.dll
hook/gbfr_hook.lib
hook/gbfr_hook.exp
hook/gbfr_hook.obj

### 其他 ###
icon.png
