# Build and development dependencies for gbfr_auto.
#
# Runtime dependencies live in requirements.txt. Nothing here is needed to *run*
# the script -- only to build the executable or lint the source.
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Same convention as requirements.txt: direct dependencies only, exactly pinned.
# Versions resolved 2026-08-23. For a fully-pinned lock including transitives:
#
#     uv pip compile requirements-dev.txt -o requirements-dev.lock

# Packaging. The README documents the --onefile build and CI runs that same
# command on windows-latest (.github/workflows/build-windows.yml).
#
# PyInstaller is not a cross-compiler -- it bundles the host platform's Python
# runtime -- so installing this on Linux lets you read the tool's help but not
# produce a Windows .exe. Build on Windows, or in CI.
pyinstaller==6.22.2

# Linter. Configured by ruff.toml, which scopes it to correctness rules rather
# than style. Kept in lockstep with the ruff-pre-commit hook in
# .pre-commit-config.yaml (rev v0.16.4) -- if you bump one, bump both.
ruff==0.16.4

# Hook runner for .pre-commit-config.yaml. Only needed if you install the hooks
# locally; the hooks themselves pin their own tool versions by rev.
pre-commit==4.6.2
